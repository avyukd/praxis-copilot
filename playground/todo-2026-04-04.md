# TODO — 2026-04-04

## P0 — CRITICAL

- [x] **0. Unified priority research queue**
  - Consolidate filing research + queue daemon into one executor
  - Priority levels: P1 (user override), P2 (scanner), P3 (queue), P4 (analyst), P5 (background)
  - Single code path through full CLAUDE.md coordinator pipeline
  - One daemon, max_parallel=4, pulls from priority queue

## HIGH PRIORITY

- [x] **1. Hourly system health + activity digest email**
  - Every hour during market hours (9 AM - 4 PM ET)
  - Summary: # filings, # PRs, # BUY/SELL/NEUTRAL, # over 0.5 → emailed
  - Detailed: every ticker with score/classification/stage
  - Serves as health ping

- [x] **2. Market cap in scanner alert emails**
  - Add market cap to `_send_alert` in local_scanner.py
  - Format: "$150M mcap" or "$2.3B mcap"

- [x] **3. Exchange-aware tickers in scanner**
  - Newsfile tickers come as "DBO" not "DBO.TO"
  - Preserve exchange suffix through full pipeline

- [x] **4. Add GNW Canadian feeds**
  - Add TSX + TSXV GNW feeds to scanner
  - URLs: /RssFeed/exchange/TSX and /TSXV

- [x] **5. Sync telemetry to S3**
  - Sync daily JSONL + calibration.yaml to S3 at end of day

- [x] **6. Calibrate capacity budget**
  - Push past 80% to hit real rate limit and calibrate
  - Then adaptive: bump 10% every 8h without rate limit

- [x] **7. Improve BUY memo email format**
  - Include full memo.yaml dump + first 2000 chars of memo.md
  - Market cap, exchange label, scannable header block

- [x] **8. Disable watchdog and thesis watch extraction**
  - Remove from analyst loop and filing research post-completion
  - Keep code, just gate it off

- [x] **9. Clean up Lambda codepaths**
  - Remove Lambda assumptions from CLI code
  - Make scanner sole source of truth

- [x] **10. Smarter specialist agent selection**
  - Coordinator decides which agents to run per company
  - Financial analyst always required, others optional
  - Remove tactical-analyst as separate agent

- [x] **11. Filter SELL/HOLD from scanner emails**
  - Already done in code, verify it's working

## MEDIUM PRIORITY

- [x] **12. Workspace is ephemeral — daemons read from S3**
  - All memo reads should fall back to S3 if workspace missing

- [ ] **13. 10-Q/10-K pipeline for small-caps** (NOT prod, gated)
  - Build behind --enable-10q flag

- [ ] **14. Codex MCP overflow** (NOT prod, exploratory)

## MEDIUM PRIORITY (blocked on user)

- [ ] **14b. IBKR + Fidelity live portfolio sync**
  - IBKR: Flex Queries (daily positions snapshot via HTTP token, no gateway needed)
  - Fidelity: Desktop scraping via IPC (no API for retail)
  - Replaces manual portfolio.yaml maintenance
  - **BLOCKED**: needs user to set up IBKR Flex Query token + provide Fidelity login for Desktop
  - Scaffolding exists in `src/cli/portfolio_sync.py`

## MEDIUM PRIORITY (IPC / Desktop)

- [ ] **15. Extract IPC into generic, transportable framework**
  - Currently lives in `src/cli/ipc.py` + MCP tools in `praxis_mcp.py`
  - Refactor into a self-contained package (e.g., `lib/claude_ipc/`) within this repo
  - Generic: task queue, results, inbox, MCP tool decorators — no praxis-specific code
  - Praxis-specific: sources.yaml, analyst integration, trading MCP tools stay in praxis
  - Should be easy to copy into another repo later or pip-install
  - Keep working in this repo for now, just clean separation

- [ ] **16. MCC + VIC authenticated browsing via Desktop**
  - MicroCapClub and Value Investors Club require login
  - Desktop uses computer use to log in, browse new writeups, extract content
  - Pipe content back via IPC inbox → analyst picks up, emails if interesting
  - Need: stored credentials (keychain or config), login flow for each site
  - Need: Desktop scheduled trigger that checks MCC/VIC daily for new posts
  - This is real alpha — these are high-quality, concentrated idea sources
  - Requires Desktop triggers to be set up first

## LOWER PRIORITY

- [ ] **17. Australian exchange support**
  - GNW ASX feeds exist but are empty (no items)
  - ASX JSON API times out from CLI (likely blocks non-browser)
  - Best approach: Desktop IPC scraping of ASX announcements page
  - Blocked on Desktop triggers being set up
- [ ] **18. Claude Desktop /usage probe**
- [ ] **19. 6b items from earlier**
